# AGENTS.md — بنّا (Banna)

راهنمای هر ایجنت/دستیار کدنویسی که روی این ریپو کار می‌کند.

## دانش‌نامهٔ توسعه (Dev Wiki) — قانون اکید
مسیر: `docs/dev-wiki/`. قوانین کامل: `docs/dev-wiki/schema.md`.

- **شروع هر نشست**: اول `docs/dev-wiki/overview.md` و `docs/dev-wiki/index.md` را بخوان؛ کل کدبیس را re-scan نکن.
- **بعد از هر تغییر معنایی کد** (مدل/سرویس/route جدید، امضای تابع تغییر کرد، dependency جدید): صفحهٔ `entities/` یا `concepts/` مربوطه را به‌روز کن یا بساز، و یک خط در `docs/dev-wiki/log.md` اضافه کن. تغییرات trivial نیاز به آپدیت ندارند؛ اگر چند فایل در یک نشست تغییر کرد، آپدیت ویکی را یک‌بار در پایان انجام بده.

## تفاوت با `docs/wiki/`
`docs/wiki/` ویکی **محصول/کسب‌وکار** است (برای GitHub Wiki و ذی‌نفعان غیرفنی) — مسئله، پرسونا، مدل کسب‌وکار، رقبا.
`docs/dev-wiki/` ویکی **کد** است (برای ایجنت/توسعه‌دهنده) — entityها، conceptها، معماری.
این دو را با هم قاطی نکن.

## نقشهٔ راه و بک‌لاگ
- نقشهٔ راه: `ROADMAP.md`
- بک‌لاگ Issueها (پیش‌نویس محلی، معادل GitHub Issues): `docs/issues/`
